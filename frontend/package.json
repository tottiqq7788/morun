{
  "name": "morun",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "cap:add:android": "cap add android",
    "copy:vosk-assets": "node scripts/copy-vosk-assets.mjs",
    "cap:sync": "npm run build && cap sync android && npm run copy:vosk-assets",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@lucide/vue": "^1.21.0",
    "vue": "^3.5.38"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@vitejs/plugin-vue": "^6.0.7",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.0",
    "vitest": "^3.2.6",
    "vue-tsc": "^3.3.5"
  }
}
